const SectionHeader2 = ({ title = "Section Title" }) => {
  return (
    <h2 className="text-3xl font-md text-dark-gray font-sk">
      {title}
    </h2>
  );
};

export default SectionHeader2;
